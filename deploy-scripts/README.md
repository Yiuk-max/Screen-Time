# Deploy Scripts

Run from a Qt-enabled terminal:

`deploy_release.bat`

Optional:

`deploy_release.bat "E:\full\path\to\ScreenTime.exe"`

The packaged app is generated under `release-package/ScreenTime`.
